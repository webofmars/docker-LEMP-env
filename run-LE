#!/bin/bash

DOCKER=$(which docker)
DATADIR="$(realpath $(dirname $0))/data"

$DOCKER run -p 'LE' -e 'USEPHP=false' -e 'USEPHALCON=false' -v ${DATADIR}/nginx/etc:/etc/nginx -v ${DATADIR}/nginx/var/www/html:/var/www/html -v ${DATADIR}/nginx/logs:/var/log/nginx webofmars/nginx:latest
